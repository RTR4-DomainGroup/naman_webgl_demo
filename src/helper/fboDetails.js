class FboDetails {

    constructor(textureWidth, textureHeight, framebuffer_obj, 
        renderbuffer_obj, framebuffer_texture)
    {
        this.textureWidth = textureWidth;
        this.textureHeight = textureHeight;
        this.framebuffer_obj = framebuffer_obj;
        this.renderbuffer_obj = renderbuffer_obj;
        this.framebuffer_texture = framebuffer_texture;
 
    }
}